;; asm.inc
;; some function finished by asm
;; so we should have a lib that asm_file can used

OCW2_M		equ	0x21
OCW2_S		equ	0xA1

EOI		equ	0x20

SELECTOR_C	equ	0x8
SELECTOR_D	equ	0x10
SELECTOR_TSS	equ	0x20